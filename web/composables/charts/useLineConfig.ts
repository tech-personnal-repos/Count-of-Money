import { useChartColors } from './useChartColors';

export const useLineConfig = {
	backgroundColor: useChartColors['hidden'],
	tension: 0.5,
	borderColor: useChartColors['blue'],
	pointRadius: 0,
	borderWidth: 2,
	borderRadius: 0
};
